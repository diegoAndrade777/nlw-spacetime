/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'media.giphy.com',
      'localhost',
      '192.168.1.10',
    ],
  },
}

module.exports = nextConfig
